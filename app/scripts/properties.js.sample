app.constant('ENV', (function() {
  var api = 'https://localhost:6660';

  return {
    API: {
      LOGIN: api + '/login',
      REGISTER: api + '/register',
      HOME: api + '/home',
    }
  }

}()));

